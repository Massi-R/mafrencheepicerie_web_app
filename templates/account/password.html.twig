{% extends 'base.html.twig' %}

{% block title %} Modifier le mot de passe - My French Epicery - {% endblock %}


{% block content %}

    {% for flash_message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ flash_message }}
        </div>
    {% endfor %}

    {% for flash_message in app.flashes('danger') %}
        <div class="alert alert-danger">
            {{ flash_message }}
        </div>
    {% endfor %}
    <h1 class="text-center mt-5">Modifier mon mot de passe</h1>
    <div class="content m-5 h-100 justify-content-center">

<a href="{{ path('app_account') }}">Retourner à mon compte </a>

{{ form(form) }}
    </div>
{% endblock %}